import { Observable } from 'rxjs/Rx';
import './rxjs';
import { Http } from '@angular/http';
export declare class Api {
    private _http;
    constructor(_http: Http);
    get<T>(requestUrl: string, additionalHeaders?: any): Observable<T>;
    /**
     * POSTs a request to the given url with the given JSON data
     * @param url
     * @param data
     */
    post<T>(requestUrl: string, data: any, additionalHeaders?: any): Observable<T>;
    private _extractData<T>(response);
    private _handleError(error);
}
